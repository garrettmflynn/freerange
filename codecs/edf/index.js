import encode from './encode'
import decode from './decode'
import config from './config'
const type = "application/x-edf"
const suffixes = "edf"

export {
    type,
    suffixes,
    config,
    decode,
    encode
}